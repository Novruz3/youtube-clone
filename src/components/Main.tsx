import { useSelector } from "react-redux"

export const Main = () => {
    // const videos = useSelector((state) => state.video.videos)
    // console.log(videos);
    
    
    return (
        <div>Main</div>
    )
}
